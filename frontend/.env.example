NEXT_PUBLIC_API_URL=/api

# Alleen nodig als je `npm run dev` los draait (niet via docker compose):
# waar de backend te bereiken is voor de server-side /api-proxy.
BACKEND_INTERNAL_URL=http://localhost:8000
